-- Install LSP servers and 3rd-party tools
-- https://github.com/williamboman/mason.nvim

return {
    "mason-org/mason.nvim",
    opts = {
        ui = {
            icons = {
                package_installed = "✓",
                package_pending = "➜",
                package_uninstalled = "✗"
            }
        }
    }
}
